const portConfig = {
    2000: {
        folder: "ecommerce",
        allowedLanguages: ["default", "nl"],
    },
    3000: {
        folder: "blog",
        allowedLanguages: ["default", "de"],
    }
}

export default portConfig;